# paywall-service

x402-protected service running on Stellar testnet.

## Responsibilities

- expose one protected route for the hackathon demo
- return `402 Payment Required` when unpaid
- return the protected result after settlement succeeds
- stay small and stable enough for repeated demo runs
